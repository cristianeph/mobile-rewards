export interface IUserValidation {
    id: number;
    secret: string;
}

export interface IUser {
    id: number;
    nombres: string;
    apellidos: string;
    cellphone: string;
    secret: string;
}